# AI decision log

> Decisions for the AI integration design, split out of
> [`AI_INTEGRATION.md`](AI_INTEGRATION.md) §8 (2026-09-15) to respect the
> file-size budget. Entries are immutable history: supersessions are recorded
> as annotations or later entries, never edits. Section references (§N) point
> into `AI_INTEGRATION.md`.

| Date | Decision |
|------|----------|
| 2026-09 | **CloudConvert retired.** The HTML→PDF conversion path in `wwwroot/scripts/resume-pdf.js` is no longer used and its `API_KEY` was removed from the repo. Final delivery of the resume is a manual print from the browser (Brave) against `/job/resume`, which is print-ready via its `@media print` rules. The file remains loaded by `Views/layout.cshtml` as dead code pending cleanup. |
| 2026-09 | **No `print` PageAction.** The extension command vocabulary (`go/open/fill/click/recheck/close/wait/reload`) stays as is; turning the resume into PDF/paper is a human step, not a browser command. |
| 2026-09 | **Resume-from-JD at the job stage is the target flow** (§4.3): regex `ResumeContext` first, LLM delta on top, human review, then print from `/job/resume`. |
| 2026-09 | **Apply-stage exception to the golden rule** (§1): a delay of a few seconds is acceptable when a single job page is in flight and a human reviews the output before printing. |
| 2026-09 | **Provider-agnostic client** (§3): plain `HttpClient` + OpenAI-compatible endpoint; local (`llama-server`, Ollama, LM Studio) or hosted (OpenRouter, Groq, DeepSeek, OpenAI, Anthropic, Gemini) chosen by config only. |
| 2026-09 | **Phase 0 topology: three stations, pull model** (§2). Core (`job-seeker`, public SSL, `X-API-Key`) is the only reachable server. The search agent's host is a long-running, outbound-only browser desktop — a station, not a server, no code changes. The AI station (GPU box) is unreachable from the core, so the AI lane runs as a **pull worker**: a new `ai-worker` console project on the AI station, using two new authenticated endpoints (`/ai/claim` with lease, `/ai/verdict`). The core never initiates AI traffic; the worker never touches SQLite directly. Tunnels (Tailscale, cloudflared, SSH reverse) were considered to restore core→AI reachability and rejected. *(The `/ai/claim`-with-lease endpoint was superseded 2026-09-11 by the read-only `GET /ai/next`.)* |
| 2026-09 | **Apply-stage mechanism amended** (§1): since the core cannot call the AI station, the apply-stage "synchronous LLM call" becomes a **priority claim** — apply-stage jobs jump the queue and `ai-worker` polls at its shortest interval while any are pending. Principle unchanged (a deliberate pause at delivery is acceptable); mechanism now matches the topology. |
| 2026-09 | **Apply assistant planned (phase 5)** ([`AI_APPLY_ASSISTANT.md`](AI_APPLY_ASSISTANT.md)): a fourth station — the personal terminal — runs a second MV3 extension that fills apply forms through an agentic tool loop (`memory_query` / `memory_write` / `fill`) against the AI station's `llama-server` reached over the LAN; the learning memory lives on the core behind new `/assistant/*` endpoints. Apply stays human-triggered (no new `JobState` in v1) and the human presses every submit. **Never both extensions in one browser** — the search extension matches `*://*/*` and would fight over the apply tabs. |
| 2026-09 | **Assistant guardrails and data sources** ([`AI_APPLY_ASSISTANT.md`](AI_APPLY_ASSISTANT.md) §3–§5): the model sees a form inventory only (never raw HTML, never emits selectors); there is no submit tool; core keys stay in the extension; personal data comes from the job's resume text in the Fill prompt plus memory — deliberately no structured profile table; submit-time diffs and chat tips are the two learning channels. An `X-Client` role header distinguishes the two extensions (absent header = legacy search). |
| 2026-09-10 | **Phase 0 is documentation-only; implementation starts at phase 1**, which absorbs `ai-worker` + `/ai/claim` + `/ai/verdict` (no separate milestone). |
| 2026-09-10 | **Four clients, three role headers.** `search`, `worker` (new — `ai-worker`, restricted to `/ai/*`), `assistant`; the dashboard user authenticates via the existing login/API-key and sends no role header. Absent header = legacy search. |
| 2026-09-10 | **`ai-worker` runs manually; no polling loop** (§2.1). The timed lease is dropped in favor of a startup sweep that resets stale in-flight jobs *(pending round-2 confirmation)*. **Superseded 2026-09-11:** no sweep, no claim, no lease — read-only `GET /ai/next` + idempotent `POST /ai/verdict`. |
| 2026-09-10 | **Apply-stage priority claim removed** (§1). The core never tracks apply stage: the user opens `job-detail`, applies on the external site, and presses the manual Apply button (`server-operations.js` `apply(jobid)`). No exception to the golden rule remains. |
| 2026-09-10 | **Two-stage purge** (§6). Score < 50 purges immediately; Attention + near-miss (50–99) keep `Html`/`Content` until the verdict; `/ai/verdict` processing purges inadequate AI ranks. Per-job AI-status encoding open (round 2). *(Resolved 2026-09-11: `AiState` encoding + human-confirmed stage 2.)* |
| 2026-09-10 | **Verdict is effective** (§4.1). `AiScore` lives in separate additive columns (regex `Score` untouched) and participates in ranking — a deliberate, careful `Q_INDEX` v2 edit; the earlier "don't touch" note is overridden. Exact formula open (round 2). *(Resolved 2026-09-11: `FinalScore = W_r * Score + W_a * AiScore` — see below.)* |
| 2026-09-10 | **Local-only across all lanes** (§3). The resume is PII and the full resume text is sent for ranking too, so hosted LLM endpoints are unused; the provider-agnostic client remains for the future. |
| 2026-09-10 | **Quality by trial and error; feedback loop agreed in principle.** No golden set. User overrides of AI rankings will be recorded and injected into future ranking prompts (design round 2). *(Settled 2026-09-11: raw override log + unified memory with hybrid injection — see below.)* |
| 2026-09-10 | **Context cap 16k tokens, configurable** (32k acceptable); tuned by trial and error. |
| 2026-09-10 | **No backfill.** Old jobs are expired; reprocessing is manual: change the job's state, run `ai-worker`. *(Superseded 2026-09-15 in wording: implementation starts from a fresh database; the spirit — never reprocess old jobs — is unchanged.)* |
| 2026-09-10 | **Duplicates are advisory** (§4.4). Dashboard warning, user-clearable; on confirmation the second posting moves to a new `Duplicated` state, which ranking must exclude. Mechanics open (round 2). *(Resolved 2026-09-11: mechanics deferred to phase 4.)* |
| 2026-09-10 | **Digest is dashboard-only statistics** (§4.5) — counts of new jobs, good jobs (regex) and strong jobs (AI verdict); no external notification channel. |
| 2026-09-10 | **Resume tailoring is selection-only end to end** (§4.3 / [`AI_RESUME_TAILORING.md`](AI_RESUME_TAILORING.md) §3). The summary becomes segmented pre-written variants; free-text summary/jobTitle generation is dropped. The user's text-box selection mechanism and `AiOptions ?? Options` fallback are unchanged. *(Partly superseded 2026-09-11: jobTitle gains a guarded title-only free-text exception, and the fallback becomes `Options.HumanEdited ? Options : (AiOptions ?? Options)`.)* |
| 2026-09-11 | **Phases 1+2 merged.** One `ai-worker` pass per job produces the semantic verdict AND the structured extraction; the former phase 2 is absorbed into phase 1, phases 3–5 keep their numbers (§7). |
| 2026-09-11 | **Two model calls per worker pass** (§6). Call 1 (always): verdict + extraction as one JSON-schema-constrained response. Call 2 (conditional, relevance ≥ threshold): resume-tailoring delta (phase 3). Both return in one idempotent `POST /ai/verdict`; rejected jobs never pay for tailoring. |
| 2026-09-11 | **Extraction storage on `Job` itself** (§4.2): additive columns `AiSalaryMin/Max`, `AiCurrency`, `AiPeriod`, `AiSeniority`, `AiWorkModel`, `AiContract`, `AiExperienceYears`, `AiSkills` (JSON, type-handler pattern); enums as names; no new table (1:1). |
| 2026-09-11 | **Final score blend** (§4.1): `FinalScore = W_r * Score + W_a * AiScore` computed at query time in `Q_INDEX` v2, never stored; initial `W_r = 0.35`, `W_a = 0.65`, weights in job-option settings (tunable, no rebuild); verdict-less jobs rank by regex `Score`. *(Settings home moved to the new `AppSetting` table 2026-09-18 — see the 2026-09-18 entries.)* |
| 2026-09-11 | **Stage-2 purge is human-confirmed** (§6): a weak verdict only marks a purge candidate; `Html`/`Content` deletion is a dashboard action (single/bulk, pre-selected by threshold). Stage 1 (< 50) stays automatic. |
| 2026-09-11 | **No claim, no lease, no sweep** (§2.1). `GET /ai/next` is read-only (oldest `Pending`, job text + resume, no state change); idempotent `POST /ai/verdict` is the only write. Crash before verdict = still `Pending`, retried naturally. One worker at a time; a second worker only duplicates inference (last-write-wins). |
| 2026-09-11 | **`Job.AiState` encoding**: additive column, enum name as text; `Pending` (queue entry: Attention + near-miss 50–99) / `Processed` (set by `/ai/verdict` after `Pending` validation); NULL = outside the queue (no backfill honored). Non-`Pending` verdicts are rejected and logged. *(Superseded 2026-09-15 by the sequential state machine — see the 2026-09-15 entries.)* |
| 2026-09-11 | **Human/AI coexistence on resume context** ([`AI_RESUME_TAILORING.md`](AI_RESUME_TAILORING.md) §6): `HumanEdited` flag in `ResumeContext` (version bump; round-trips through `SimlpeSerialize`); precedence `Options.HumanEdited ? Options : (AiOptions ?? Options)`; AI writes only `AiOptions`; post-edit AI suggestions show as a diff with an explicit accept action. |
| 2026-09-11 | **jobTitle: fixed variants + guarded title-only free text** ([`AI_RESUME_TAILORING.md`](AI_RESUME_TAILORING.md) §3): default is selection among pre-written headline variants; the model may propose free text for the title slot only — review-flagged, never auto-applied, server-side single-line/length cap. Raw `job.Title` copying is dropped. |
| 2026-09-11 | **`Duplicated` mechanics deferred to phase 4**; the advisory-only decision stands. |
| 2026-09-11 | **Unified memory subsystem** ([`AI_APPLY_ASSISTANT.md`](AI_APPLY_ASSISTANT.md) §4): one table/API/retrieval mechanism with a `Scope` column (`resume`/`apply`/`ranking`), replacing the planned `apply_memory`; ranking keeps a separate raw override log, durable lessons distill into `Scope = ranking` rows. |
| 2026-09-11 | **Hybrid memory injection** ([`AI_APPLY_ASSISTANT.md`](AI_APPLY_ASSISTANT.md) §4): deterministic pre-injection of high-confidence rows as a labeled, token-capped (~1–2k), stably-ordered data block + `memory_query` for exploration. Memory is data, never instructions; `UseCount` bumps only on applied values; chat writes visible and deletable; page text never enters memory unconfirmed. |
| 2026-09-11 | **`X-Client: search` ships in phase 1** — one-line extension change, single deployment; `X-API-Key` unchanged. |
| 2026-09-11 | **Assistant-mediated feedback UX** ([`AI_APPLY_ASSISTANT.md`](AI_APPLY_ASSISTANT.md) §4): resume-tailoring and apply-form feedback both flow through the assistant chat; the model decides what to persist and instructs the assistant to write memory rows for future prompts. |
| 2026-09-12 | **Near-miss retention is a phase-1 code change** (§6). Today's purge threshold is `Score < MinEligibilityScore = 100` — the earlier "below 50, today's behavior unchanged" claim was wrong. The new stage-1 threshold is the configurable near-miss floor (e.g. 50/70), shared with AI-queue entry and shipped together with `AiState = Pending` in `EvaluateJobEligibility`; expected DB growth accepted; the Attention pass-mark stays 100 (the floor never changes `JobState`). *(Floor default fixed at 70 and made a state boundary 2026-09-15 — see below.)* *(AiPending growth re-accepted 2026-09-18: the safety-net purge proposal was rejected — age-based `Q_CLEAN` stays the only bound.)* |
| 2026-09-12 | **Verdict semantics are upsert** (§2.1), superseding the 2026-09-11 "non-Pending verdicts rejected" rule: `POST /ai/verdict` always updates after validating job existence and payload (AiScore 0–100, enum names, length caps); out-of-queue verdicts are logged informationally. Resolves the idempotent/last-write-wins contradiction; retry-after-lost-response is a no-op; manual reprocess = re-run the worker. |
| 2026-09-12 | **AiScore fixed at 0–100; FinalScore normalized** (§4.1): the model judges apply-worthiness (master resume + JD + JobOption weights); `RegexNorm = min(Score, ScoreCap)/ScoreCap×100` (`ScoreCap` setting, initial ~300); `FinalScore = W_r×RegexNorm + W_a×AiScore` on 0–100; decay applied post-blend; the `JobRanking.cs` mirror is updated in the same change. Regex-vs-AI divergence statistics are dropped (scale mismatch). |
| 2026-09-12 | **Ranking profile = master resume** (§4.1): the verdict prompt carries a stable, job-independent base resume. Open (round 3): storage source — settings text vs rendered default template. *(Resolved 2026-09-15: template-derived in-memory cache — see below.)* |
| 2026-09-12 | **Second-chance threshold is a setting** applied to `FinalScore` (0–100 scale). Open (round 3): promotion mechanics — dynamic top category in `Q_INDEX` v2 vs `JobState` change. *(Resolved 2026-09-15: promotion is the verdict gate, not the blend — see below.)* |
| 2026-09-12 | **`llama-server` stays localhost through phase 3** (§3); LAN exposure (binding/proxy + home-LAN trust assumption) is decided in phase 5 — resolves the §2/§3 contradiction. **One worker at a time** is a standing convention, not enforced in code. |
| 2026-09-12 | **Open (round 3) queue:** master-resume storage source; near-miss promotion mechanics; confirmation of upsert guards + `ScoreCap` initial value. Deferred: queue membership rules (when `Pending` is set/exited, Revaluation re-queue, oldest-first key), poison-job error verdict, manual-reprocess dashboard action, roles-are-not-a-security-boundary note (cookie auth = dashboard), proxy hardening (rate limit / HSTS / IP allowlist for `/ai/*`), worker config core `ApiKey` + deployment story. *(Resolved 2026-09-15 — see the 2026-09-15 entries below.)* |
| 2026-09-15 | **Sequential state machine replaces the `AiState` column** (§4.1). The regex gate writes `NotApprovedRegex` (below the floor, content purged) or `AiPending` (floor+, both bands); the verdict gate writes `Attention` (`AiScore ≥ AiPassmark`) or `NotApprovedAI`. Queue membership is `State = AiPending`. Enum order `Saved, Revaluation, NotApprovedRegex, AiPending, NotApprovedAI, Attention, Rejected, Applied`; the order-dependent `user_changes` guard becomes an explicit `is Rejected or Applied` check. Browser follow-up commands re-key from `Attention` to the regex-approval result. Supersedes the 2026-09-11 `AiState` encoding and resolves the round-3 promotion-mechanics question. Implementation starts from a **fresh database** (old jobs expired) — no row migration; the no-backfill decision's spirit (never reprocess old jobs) is unchanged. |
| 2026-09-15 | **No bypass.** `Attention` is reachable only via a verdict: no AI-off fallback, no manual promote. The top list stays empty until the worker runs — accepted. *(Superseded 2026-09-18: emergency manual promote — see the 2026-09-18 entries.)* |
| 2026-09-15 | **Round-3 settings confirmed:** near-miss floor **70**, `AiPassmark` **60**, `ScoreCap` **300**, weights `W_r = 0.35` / `W_a = 0.65` (either may be 0 — nullify or amplify either score). `/ai/verdict` guards: job exists; AiScore 0–100; valid enum names; reason ≤ 2000 chars; skills ≤ 20; salary ≥ 0; experience 0–50; verdict columns always upserted, state transition only from `AiPending`. |
| 2026-09-15 | **Blend scope** (§4.1): `FinalScore` orders only verdict-bearing states (`Attention`, `NotApprovedAI`); `AiPending` ranks by `RegexNorm` alone. Re-queued jobs keep prior verdict columns visible but unused — no destructive clear. |
| 2026-09-15 | **Master resume = template-derived cache** (§3): render `resume.cshtml` with a fixed default context (English, full skills), prune + strip server-side, cache in memory (lazy, restart-only invalidation), 16k cap. Resolves the round-3 storage-source question. |
| 2026-09-15 | **Queue rules resolved** (§2.1): revaluation after a settings change re-queues every floor-passing job (full re-queue); a re-scrape re-queues only when the scraped text changed; queue order is oldest-first by `JobID`. *(Revaluation scope narrowed 2026-09-18 — AI-domain, content-bearing states only; see the 2026-09-18 entries.)* |
| 2026-09-15 | **Poison handling + re-queue button** (§6): two retries per run, then an error verdict → `NotApprovedAI` with the error as reason; a dashboard "re-queue" action sets a job back to `AiPending`. |
| 2026-09-15 | **Ops:** `/ai/*` hardening is documentation only (reverse-proxy snippet: rate limit, HSTS, optional IP allowlist) — no in-app limiter. `X-Client` documented as routing, not authorization (§2). Worker config: committed `appsettings.json` skeleton + env vars (`Llm:BaseUrl/Model/Core/ApiKey`), `dotnet publish` + copy deployment; the shared `X-API-Key` stays single — per-client keys deferred to phase 5. *(Superseded 2026-09-18: per-client keys ship in phase 1 and `X-Client` becomes informational only — see the 2026-09-18 entries.)* |
| 2026-09-17 | **Phase-1 UI minimal; phase 6 created for dashboard side-work.** Phase 1 ships only extracted-value display, `AiScore` display and the re-queue button. Filters over the extraction columns (design + implementation), the bulk-purge action, the digest-count redefinition (supersedes the "when phase 4 lands" follow-up) and any other dashboard side-work are deferred to a **new phase 6** in the phasing table (§7); phases 3–5 keep their numbers. *(Phase-6 digest scope extended 2026-09-18: + an `AiPending` counter.)* |
| 2026-09-17 | **AI enum member sets locked** (§4.2): `AiVerdict`: `StrongMatch, Match, Possible, NoMatch, Error`; `AiSeniority`: `Junior, Mid, Senior, Lead, Unknown`; `AiWorkModel`: `Onsite, Hybrid, Remote, Unknown`; `AiContract`: `Permanent, B2B, Temporary, Unknown`; `AiPeriod`: `Hour, Day, Month, Year, Unknown`. Identical sets in three places: the model's JSON schema, the `/ai/verdict` validation, and the DB columns (enum names as text). `Error` exists for poison jobs. |
| 2026-09-17 | **Rubric v1 recorded; future home = ai-worker `Llm:Rubric` appsettings** (§4.1): the v1 system-prompt text lives in [`AI_PHASE1_NOTES.md`](AI_PHASE1_NOTES.md), tunable without rebuild (trial-and-error is the agreed tuning method). `GET /ai/next` will carry a JobOption-derived `keywords` field; the worker injects it at the `{{keywords}}` placeholder — realizing the "master resume + JD + JobOption weights" input (2026-09-12). |
| 2026-09-17 | **Re-scrape content-change test = simple normalization** (§2.1): `content_changed = Normalize(newText) != Normalize(stored Content)`, where Normalize collapses consecutive whitespace to a single space and trims ends. No hash column. Applied in `JobPage.LoadJob`, mirrored in `StepstonePageJob`. |
| 2026-09-17 | **Floor-lowering resurrection → `Saved`.** In `RunRevaluateProcess`, a purged `NotApprovedRegex` job whose stored `Score` passes the new floor is set back to `State = Saved` (purge nulls only `Html`/`Content`; `Score` survives). No new browsing mechanism: the browser re-scrapes naturally iff the job still appears in search results (a proxy for "still open"); expired listings do not return — accepted. Content-bearing jobs re-queue to `AiPending` per the 2026-09-15 rule. *(Amended 2026-09-18: the resurrection also writes `Attempts = 0, Tries = NULL` — unified Attempts-reset law.)* |
| 2026-09-17 | **`AIError` state for poison jobs** (§4.1), amending the 2026-09-15 poison rule (error verdicts now land in `AIError`, not `NotApprovedAI`). Enum order: `Saved, Revaluation, NotApprovedRegex, AiPending, NotApprovedAI, AIError, Attention, Rejected, Applied`. An error verdict writes `AiVerdict = Error` + the error text as `AiReason` and bypasses the passmark gate. Derived rules: excluded from `FinalScore` ordering (own category by `RegexNorm`, `Q_INDEX` v2 + `JobRanking.cs`); purge candidacy and `Q_CLEAN_NOT_APPROVED` cover `AIError`; re-queue button `AIError → AiPending`; a changed re-scrape re-evaluates it (counts as un-judged); a later valid verdict upserts columns without a state change (out-of-queue informational log). |
| 2026-09-17 | **Worker ops: 120 s timeout; connection vs output errors split** (§6): connection failures (refused/DNS/timeout before any response) abort the whole run and write **nothing** — a GPU outage must never mass-produce verdicts; model-output failures (invalid JSON/schema) retry ×2 within the run, then one error verdict for that job only (`AIError`). Context budget under the 16k cap: rubric (~1k) + master resume in full + JD remainder; an over-long JD is truncated at its **tail** (requirements live early) — resolving the ambiguous "truncate from the top" wording. |
| 2026-09-17 | **Forward-compatibility constraints recorded** ([`AI_PHASE1_NOTES.md`](AI_PHASE1_NOTES.md) F1–F6): prompts are assembled worker-side from labeled blocks (`/ai/next` ships data only; a phase-5 memory-injection slot is reserved); `/ai/verdict` validation ignores (never rejects) an absent `delta` (phase 3 activates it); `X-Client` role registration is data-driven (phase-5 `assistant` = a registration, not a rewrite); no order-dependent `JobState` logic anywhere (phase-4 `Duplicated` stays safe to add); the master-resume prune/strip is built as a reusable helper (phases 3/5 reuse it); the single shared `X-API-Key` stands (per-client keys are phase 5). *(F6 superseded 2026-09-18: per-client keys ship in phase 1 — see the 2026-09-18 entries.)* |
| 2026-09-17 | **Phase-3 ambiguities resolved** ([`AI_RESUME_TAILORING.md`](AI_RESUME_TAILORING.md), [`AI_INTEGRATION.md`](AI_INTEGRATION.md) §6): (A) the tailoring call runs only when the verdict promotes to `Attention` — threshold = `AiPassmark`, no new setting; the value ships in `GET /ai/next`. (B) Call-2 inputs extend the `/ai/next` payload (per F1): `settings` (`aiPassmark`), the per-job `options` converted to standard JSON (the worker never sees the simple-JSON format), and the template-derived block `inventory` (stable id = selector, type, `key-*` tags, short excerpt; cached in memory like the master resume). (C) The delta contract is selection-only — `keys`, `included`/`notIncluded` selectors, `length`, summary/title selection via template elements; `InputData`, `Elements`, `PageBreak` are human-only. The core applies the delta onto `Options` and stores the complete context in `AiOptions`; the raw delta goes to `job.Log`. Validation: keys ⊆ `MainKeys`; selectors ⊆ inventory (whitelist, not syntax-only); length ∈ {1,2}; segments/variants ⊆ template ids; free-text title single line ≤ 80 chars; caps ≤ 8 keys / ≤ 30 selectors. An invalid delta is dropped — the verdict still applies; the core likewise drops a delta arriving with a non-promoting or `Error` verdict; a call-2 model-output failure retries twice, then the verdict posts alone (connection failures abort the run per the worker-ops rule). (D) Summary segments and title variants are template elements on the existing levers — the `summaryOn`/`titleVariant` context fields are dropped and the custom serializer stays untouched; the Persian summary stays generic in v1. (E) Supersedes the AI_RESUME_TAILORING §4 "approved before `Applied`" guardrail: selection deltas are live immediately; review is the user's duty on the job-detail page (the only UI path to the resume — `job-detail.cshtml`). The free-text title alone stays gated: pending in an additive `Job.AiTitle` column (≤ 80 chars) until explicit acceptance; accept-after-human-edit is a full copy (sets `HumanEdited`); a re-run rewrites `AiOptions` unless `Options.HumanEdited`. (F) Re-demoted (`NotApprovedAI`/`AIError`) jobs keep their delta — no destructive clear; the 16k cap applies per call; call 2 = rubric + inventory + context + JD, tail-truncated, assembled worker-side per F1. Design only — no code yet. |
| 2026-09-18 | **Revaluation scope narrowed + Attempts-reset law** (§2.1, D1/D4): `Q_FETCH_FROM`/`Q_FETCH_FROM_COUNT` use a positive list — `State IN ('Attention','AiPending','NotApprovedAI','AIError') AND Content IS NOT NULL AND ModifiedOn <= @date` (the old `State != 'Revaluation'` condition is absorbed); `Saved`, `Rejected`, `Applied` (and future `Duplicated`) are excluded — terminal/user-decided jobs keep Score/Log frozen as a historical record. `ResetRevaluations` becomes `UPDATE Job SET State = 'Saved', Attempts = 0, Tries = NULL WHERE State = 'Revaluation'` — since `Saved` is excluded from the fetch, crashed-run leftovers recover via browser revisit, and the Attempts reset avoids the `Q_FETCH_FIRST` `Attempts >= 4` zombie trap. Unified law: **any deliberate return to `Saved` for reprocessing resets `Attempts`** (the 2026-09-17 resurrection amended accordingly). Phase-3 law recorded now: a `HumanEdited` guard in `EvaluateJobEligibility` — when `Options.HumanEdited == true`, skip the `Options` overwrite (`Score`/`Log` still regenerate) on every eval path except the per-job force button. |
| 2026-09-18 | **Per-job Revaluate button** (§2.1, D1.4): `JobController.Revaluate` gains an optional `jobid` query param. Single-job mode = force re-eval: ignores the `HumanEdited` guard (overwrites `Options` with the fresh regex context), keeps the explicit `Rejected`/`Applied` guard, and — operative from phase 3 — clears `AiOptions`/`AiTitle` so nothing stale renders under the precedence rule. Disabled/hidden when `Content == null`. Accepted: in phase-1 semantics a floor-passing job returns to `AiPending` (leaves `Attention` until the next worker run). No param = the existing global process, unchanged; no locking vs a concurrent global revaluation (regex is deterministic; last write wins). |
| 2026-09-18 | **Verdict fingerprint** (§2.1, D2): `GET /ai/next` adds `fingerprint` = SHA-256 hex of the whitespace-normalized `Content` (the 2026-09-17 Normalize) — computed per request, nothing stored (no hash column). `POST /ai/verdict` echoes it; the core recomputes from the currently stored `Content` at verdict time. Match → normal gate. Mismatch (content changed between fetch and verdict) → upsert verdict columns + informational log, **no state transition** — the job stays `AiPending` and is re-judged on the next worker run. `Content == null` at verdict time = mismatch. Closes the `/ai/next` ↔ `/ai/verdict` race. |
| 2026-09-18 | **`AppSetting` table is the settings home** (§4.1/§6, D3): new `database/structure/app-setting.sql` — `AppSetting (Key TEXT PRIMARY KEY, Value TEXT)` + seeds `floor=70`, `aipassmark=60`, `scorecap=300`, `w_regex=0.35`, `w_ai=0.65` (`installation.sh` picks up `structure/` files automatically). New `AppSettingBusiness` (typed getters, defaults for absent keys, **no cache** — read per use). Resolves the "job-option settings" ambiguity: the five phase-1 settings are global, not job-option rows; edited via the existing job-options SQL console (`POST /job/setting`). Consumers: the regex gate (floor — replaces the `MinEligibilityScore` const in both usages), `/ai/next` (`aipassmark`, phase 3), `Q_INDEX` v2 + `Q_CLEAN_ATTENTION` (scorecap + weights as SQL parameters). |
| 2026-09-18 | **`Q_CLEAN_ATTENTION` keys on `FinalScore`** (§4.1, D5): the top-100 Html-retention subquery orders by the v2 blend (`FinalScore = W_r × RegexNorm + W_a × AiScore`, weights from `AppSetting`) instead of raw `Score`; the blend expression is a shared C# SQL fragment used by both `Q_INDEX` v2 and `Q_CLEAN_ATTENTION`. |
| 2026-09-18 | **Deterministic verdicts** (§6, D6): worker `Llm` config gains `Temperature` (default 0.2) and `Seed` (fixed value), sent with every request (`llama-server` supports both). Re-runs must be stable so `AiScore` cannot flip around `aiPassmark` without an input change. |
| 2026-09-18 | **Per-client API keys in phase 1** (§2, D7) — supersedes "roles are routing, not authorization" (2026-09-15) and F6 (2026-09-17). Keys in config/env, **not** DB: `Auth:ApiKeys:Dashboard`, `Auth:ApiKeys:Search`, `Auth:ApiKeys:Worker` (`Assistant` is added in phase 5); the single `Auth:ApiKey` mode is **removed** (clean cutover, single-user system). **Key = role with path rules** in `Program.cs`'s `Authorized` middleware: `search` → `/decision/*` (+ `/decision/scopes`), `worker` → `/ai/*`, `dashboard` → everything; FixedTimeEquals per key; `X-Client` stays an informational/logging header only. Dashboard cookie login unchanged; login password = the Dashboard key. Production fail-fast: only a missing **Dashboard** key refuses startup; missing Search/Worker keys log a warning. Development behavior (all optional, auth disabled) unchanged. Extension: the user pastes the Search key into the existing popup API-Key field; worker config gains `Llm:CoreApiKey` (the core's worker key) alongside `Llm:BaseUrl/Model/Core`. Reverse-proxy hardening (2026-09-15) stands as a documented recommendation. |
| 2026-09-18 | **Emergency manual promote** (§4.1, D8) — supersedes "No bypass" (2026-09-15). Dashboard button on job-detail (`POST /job/promote?jobid=`): allowed only from `AiPending`, `NotApprovedAI`, `AIError` → `Attention`; never from `Rejected`/`Applied` (or future `Duplicated`). No synthetic `AiScore` — within the `Attention` category, jobs without a verdict rank by `RegexNorm` (COALESCE-style in `Q_INDEX` v2). Audit: append `Manually promoted (emergency) — <date>` to `job.Log`; bump `ModifiedOn`. Finality: the promoted job leaves the queue; the worker will not see it again until a changed re-scrape or a revaluation re-queues it — accepted. |
| 2026-09-18 | **Queue inflation accepted, visibility deferred** (§7, D9): no change to `EvaluateSalaryScore`; the phase-6 digest work gains an `AiPending` counter so floor tuning (via `AppSetting`) is data-driven. |
| 2026-09-18 | **Phase-5 topology: the assistant runs on the AI station** (§2, [`AI_APPLY_ASSISTANT.md`](AI_APPLY_ASSISTANT.md) §1) — resolves the deferred LAN-exposure question (2026-09-12) with zero infrastructure: the "personal terminal" is the AI station machine itself, so the assistant extension reaches `llama-server` at `http://localhost:<port>` and `llama-server` stays localhost-bound **permanently** — no interface binding, no reverse proxy, no home-LAN trust assumption. The search terminal stays a separate machine; the never-both-extensions-in-one-browser rule stands. Consequence: assistant and worker share one host and one `llama-server` (next entry). The §2 diagram is amended accordingly. |
| 2026-09-18 | **Phase-5 model sharing: one `llama-server`, `--parallel 2`** (§3) — worker (batch, overnight) and assistant (interactive fill) are two independent clients of the same instance; neither mediates for the other. Two parallel slots remove queue-behind-batch latency. Deployment note: with `--parallel`, the total context `-c` splits across slots — provision ≥ ~32k total so the worker slot keeps its 16k budget (the assistant needs ~8k). The one-worker-at-a-time convention (2026-09-12) is unchanged. |
| 2026-09-18 | **Assistant key path rules** (§2, extends D7) — the phase-5 `Auth:ApiKeys:Assistant` key is allowlisted to `/assistant/*` (all methods: jobs list, applied report, memory CRUD) and `GET /decision/scopes`; no `/ai/*`, no `/decision/take`. Path-based rules only — `/decision/scopes` is a GET-only route, so no per-method middleware machinery is needed. Implemented as a registration in the data-driven role table (F3, [`AI_PHASE1_NOTES.md`](AI_PHASE1_NOTES.md)), not a middleware rewrite. |
| 2026-09-18 | **Memory stays plaintext in v1; encryption deferred beyond v1** ([`AI_APPLY_ASSISTANT.md`](AI_APPLY_ASSISTANT.md) §4) — replaces the ambiguous "deferred to the final phase" wording (2026-09-10). Accepted risk: protecting `data.sqlite3` is a disk/file-security concern; revisited alongside the other §7 out-of-scope hardening (post-v1). |
| 2026-09-18 | **Dual `Applied` paths, both idempotent** ([`AI_APPLY_ASSISTANT.md`](AI_APPLY_ASSISTANT.md) §6) — the dashboard manual apply button (`POST /job/apply` → `ChangeState(Applied)` — today writes no Log entry) and `POST /assistant/applied {jobId}` both set `State = Applied`; repeat calls are harmless. A `job.Log` line records the source (`applied via dashboard` / `applied via assistant`) — a phase-5 build change extending today's Log-less `ChangeState`. Resolves the "phase-5 integration is revisited in the final phase" note in [`AI_RESUME_TAILORING.md`](AI_RESUME_TAILORING.md) §6. |
| 2026-09-18 | **Memory hygiene: deterministic cap, model shapes content by writes only** ([`AI_APPLY_ASSISTANT.md`](AI_APPLY_ASSISTANT.md) §4) — hard row cap per `Scope` via a new `memorycap` AppSetting (default 500, the D3 `AppSetting (Key, Value)` pattern); on a write past the cap the core prunes the lowest-value row: lowest `UseCount`, then oldest `UpdatedAt`. The model gets **no delete tool** — stale facts are superseded via `memory_write` plus the existing precedence rule (correction > tip), and the cap retires dead rows; manual delete via the memory CRUD API remains. |
| 2026-09-18 | **`Q_INDEX` v2 dashboard layout (D11)** (§4.1): category numbers + display caps — `Attention = 1` (12 rows, `FinalScore`; verdict-less promoted jobs COALESCE to `RegexNorm`), `AiPending = 2` (6, `RegexNorm`), `NotApprovedAI = 3` (6, `FinalScore`), `Applied`/`Rejected = 4` (3 each, unchanged), `NotApprovedRegex = 5` (6, `RegexNorm`), `AIError = 6` (3, `RegexNorm`), ELSE `Saved`/`Revaluation = 12` (1, unchanged). Page order top→bottom: Attention, AiPending, NotApprovedAI, Applied/Rejected, NotApprovedRegex, AIError, Saved/Revaluation — actionable rows (AI-approved, queue) on top; bulk/informational bands below. Folded into [`AI_PHASE1_NOTES.md`](AI_PHASE1_NOTES.md) (`JobBusiness` section). |
| 2026-09-18 | **Re-queue button source states + content guard (D12)** (§2.1/§6): the dashboard re-queue action allows only `AIError` and `NotApprovedAI` → `AiPending` — never from `Attention` (the per-job Revaluate button, D1.4, already covers its return to the queue), `Rejected`, `Applied`, or `Saved`. Resolves the "`RequeueJob` (from AIError **and others**)" ambiguity in [`AI_PHASE1_NOTES.md`](AI_PHASE1_NOTES.md). Content guard: the button is disabled/hidden when `Content == null` (mirrors the D1.4 guard — the manual `Clean` button's 7-day `Q_CLEAN_NOT_APPROVED` purges the content of exactly these two states; `JobController.Clean` is its only caller, no timer), and `FetchNextAiPending` filters `Content IS NOT NULL` defensively — a contentless job must never reach a verdict (consistent with D2's "`Content == null` at verdict time = mismatch"). |
| 2026-09-18 | **ai-worker core-error protocol, loop-safety (D13)** (§2.1/§6): `GET /ai/next` — any network error or non-200 aborts the whole run; `POST /ai/verdict` — 404 (job deleted between fetch and verdict) → log + continue with the next job; 400 (validation rejection = worker bug) → abort the run loudly; 5xx or network error → abort the run. Prevents an endless loop on the same oldest `AiPending` job. |
| 2026-09-18 | **Call-2 (tailoring) rubric (D14)** (§6): config key `Llm:RubricTailor` in the worker's `Llm` section (env-overridable like `Llm:Rubric`), same shared `Temperature`/`Seed` (D6). A v1 draft is recorded in [`AI_PHASE1_NOTES.md`](AI_PHASE1_NOTES.md) ("RubricTailor v1"). The draft consumes `{{keywords}}` — a deliberate extension of the declared call-2 input list (rubric + inventory + context + JD): legal because `keywords` ships in `/ai/next` from phase 1 and is a constant (the global `JobOption` table), so the call-2 prefix stays cache-stable. |
| 2026-09-18 | **`keywords` payload format (D15)** (§2.1): `GET /ai/next` returns `keywords` as a standard JSON array of `{category, score, title}` objects derived from `JobOption`; the `reject` category is excluded (regex-side concern, wasted tokens); stable cached order (`FetchAll` order) for determinism. |
| 2026-09-18 | **Inventory caps, call 2 (D16)** (§3/§6): per-item text excerpt ≤ 120 chars; no item-count cap — the template is finite and bounded by design, and the existing JD tail-truncation absorbs any 16k overflow. |
| 2026-09-18 | **`Llm:Enabled` removed (D17)** (§3): dropped from the worker config skeleton — the worker runs manually (§2.1); a kill-switch is a dead code path. |
| 2026-09-18 | **Correction (D3 annotation): `installation.sh` does NOT pick up `structure/` files automatically.** The script enumerates each file explicitly (`agency.sql`, `job-option.sql`, `job.sql`, `trend.sql`, `passwords.sql`); the D3 "picked up automatically" wording was wrong. The phase-1 implementation must add `sqlite3 data.sqlite3 < structure/app-setting.sql;` to `database/installation.sh` — without the line the settings table never exists and the floor/passmark reads silently fall back to defaults. (Also note `installation.sh` uses `create table if not exists`, so re-running it never adds columns to an existing table — relevant to the open item below.) The D3 decision itself — `AppSetting` table as the settings home — is unchanged. |
| 2026-09-18 | **OPEN — rollout granularity (user decision pending; deliberately not chosen):** phases 1–3 in one release vs separate releases. *Together (simplest):* `AiOptions`/`AiTitle` enter `database/structure/job.sql` from day one; the existing fresh-DB decision covers everything — **no migration needed at all**. *Separately:* pick a migration path for the live DB (recommended: one-off documented `ALTER TABLE` via the existing `POST /job/setting` SQL console; alternative: startup `PRAGMA table_info` + `ALTER` helper). Everything else in phases 1–3 is decided. |
